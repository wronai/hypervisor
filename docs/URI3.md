# uri3

`uri3` jest niezależną paczką do pracy z URI: parser, normalizer, resolvery, scanner usług HTTP/A2A-like, graf zależności i walidacja URI Tree.

Hypervisor powinien zależeć od `uri3`, ale `uri3` nie powinno zależeć od hypervisora.
