"""Operator adapters live in ``agents/operators/*/adapters/``.

The union registry in ``operation_registry/registry.yaml`` references those modules
directly. This package is kept only as a namespace placeholder.
"""

