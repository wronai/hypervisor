# TODO

- [ ] Replace internal default resolver with real `uri3` Operation Registry lookup when integrated.
- [ ] Add `uri3 expand-flow` wrapper command.
- [ ] Add `nl2uri flow -p ...` output mode.
- [ ] Add schema validation with optional `jsonschema` dependency.
- [ ] Add condition normalization tests for richer boolean expressions.
- [ ] Add support for groups/parallel blocks.
