{% include '_dockerfile_header.j2' %}
FROM python:3.12-slim
WORKDIR /app
COPY . /app
RUN pip install --no-cache-dir -e .
EXPOSE {{ default_port }}
CMD ["uvicorn", "agents.generated.{{ spec.python_package }}.main:app", "--host", "0.0.0.0", "--port", "{{ default_port }}"]
